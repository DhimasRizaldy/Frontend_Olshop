import FormChange from "../../components/Fragments/Auth/FormChange";
import AuthLayouts from "../../layout/AuthLayouts";

const ChangePage = () => {
  return (
    <AuthLayouts type="change-password">
      <FormChange />
    </AuthLayouts>
  );
};

export default ChangePage;
